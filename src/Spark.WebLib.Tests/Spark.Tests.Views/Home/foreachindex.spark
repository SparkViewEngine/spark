<div>
  <var Names='new[] {"Alpha", "Beta", "Gamma", "Delta"}' Styles='new[] {"even", "odd"}'/>

  <for each="var name in Names">
    <p>${nameIndex}: ${name}</p>
  </for>

  <ul>
    <li each="var name in Names" class='${Styles[nameIndex % 2]}'>${name}</li>
  </ul>
</div>

