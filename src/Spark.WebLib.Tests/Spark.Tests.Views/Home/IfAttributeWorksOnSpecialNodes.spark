<content name="Hello" each="var name in new[]{'alpha', 'beta', 'gamma'}" if="name != 'beta'">
  <p>name-${nameIndex}-${name}</p>
</content>
<use content="Hello"/>
<SimpleValue each="var foo in new[] {'one','two','three'}" if="foo != 'two'"/>
